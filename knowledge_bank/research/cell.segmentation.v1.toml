type = "research"
signature = "cell.segmentation.v1"
name = "Cell Segmentation"
topic = [
  "cell segmentation",
  "method comparison",
  "stardist",
  "cellpose",
  "best practices",
]

description = """This workflow provides a practical framework for comparing a classic image analysis technique (Otsu thresholding with Watershed) against two state-of-the-art deep learning models (StarDist and Cellpose) for cell nuclei segmentation. By generating both quantitative results (CSV files) and a qualitative visual comparison (2x2 montage), users can effectively evaluate the performance of each method on their specific dataset. The classic method serves as a fast, accessible baseline, while StarDist and Cellpose offer more robust segmentation, particularly in handling clustered or touching cells, at the cost of requiring a configured external Python environment. This comparative approach is a best practice for validating and selecting an appropriate segmentation algorithm."""

summary = """Here is a detailed summary of the ImageJ task execution steps.

### Summary of ImageJ Task

**1. Task Overview**
The main objective was to perform cell segmentation on a given image using three distinct methods: classic thresholding, StarDist, and Cellpose. The task required saving the segmentation results (labeled images and measurement data in CSV format) for each method and creating a final 2x2 comparison montage showing the original image alongside the three segmented images.

**2. Key Actions Taken**
The task was executed through a structured, multi-step workflow involving initial analysis, user-approved planning, and a combination of native ImageJ macros and delegated Python tasks.
*   **Image Analysis (`imagej_perception`)**: The process began by analyzing the input image (`original_image.tif`) to identify its properties. It was determined to be an 8-bit grayscale image containing bright, distinct nuclei against a dark background, which is ideal for segmentation.
*   **Planning & User Approval (`user_manipulation`)**: A comprehensive four-step plan was formulated and presented to the user. This plan outlined the classic segmentation in ImageJ, delegation of StarDist and Cellpose to a Python Agent, and the final creation of a comparison figure. The user approved this plan before execution.
*   **Classic Segmentation (`run_macro`)**: An ImageJ macro was executed to perform threshold-based segmentation. This involved duplicating the image, applying an automatic Otsu threshold, using the Watershed algorithm to separate touching nuclei, and running "Analyze Particles" to generate a labeled image and a results table.
*   **Delegation to Python Agent (`delegate_task`)**: For the deep learning-based methods, the task was delegated to the `Python Agent`, which has specialized tools for StarDist and Cellpose.
    *   **StarDist Segmentation**: The image path and description were sent to the agent, which performed the segmentation and returned the labeled image and CSV results.
    *   **Cellpose Segmentation**: The process was repeated for Cellpose, with the agent executing the model and saving the required output files.
*   **Montage Creation (`run_macro`)**: A final ImageJ macro was run to open the original image and the three generated labeled images. These were combined into a 2x2 montage with clear labels and saved as a single figure.

**3. Critical Results**
The task successfully generated all requested outputs, providing a comprehensive comparison of the segmentation methods.
*   **Segmentation Data**: Three CSV files were created, containing detailed measurements (area, position, etc.) for the objects identified by each method:
    *   `classic_results.csv`
    *   `stardist_results.csv` (341 nuclei detected)
    *   `cellpose_results.csv`
*   **Labeled Images**: Three TIFF images showing the segmentation masks were saved:
    *   `classic_segmentation.tif`
    *   `stardist_segmentation.tif`
    *   `cellpose_segmentation.tif`
*   **Comparison Figure**: A final montage image, `comparison_montage.tif`, was created, visually comparing the performance of the three methods against the original image.

**4. Error Handling**
The workflow was executed without errors. The strategy of delegating specialized deep learning tasks to the Python Agent was a key part of the plan, proactively avoiding potential issues with complex scripting or missing libraries within the standard ImageJ environment. This approach ensured a smooth and successful execution.

**5. Current State**
The task is **complete**. All segmentation operations have been performed, all requested files have been saved to the specified directory, and the final comparison figure has been generated.

**6. Technical Details**
*   **Input Image Path**: `D:/vscode_project/ChatImageJ/temp/original_image.tif`
*   **Classic Segmentation Method**: Otsu automatic thresholding followed by the Watershed algorithm. Particle size was filtered for `100-Infinity` pixels.
*   **Deep Learning Models**: StarDist and Cellpose, executed via the Python Agent.
*   **Final Output Paths**:
    *   Results: `D:/vscode_project/ChatImageJ/temp/classic_results.csv`, `.../stardist_results.csv`, `.../cellpose_results.csv`
    *   Images: `D:/vscode_project/ChatImageJ/temp/classic_segmentation.tif`, `.../stardist_segmentation.tif`, `.../cellpose_segmentation.tif`
    *   Montage: `D:/vscode_project/ChatImageJ/temp/comparison_montage.tif`"""


content = """This workflow provides a practical framework for comparing a classic image analysis technique (Otsu thresholding with Watershed) against two state-of-the-art deep learning models (StarDist and Cellpose) for cell nuclei segmentation. By generating both quantitative results (CSV files) and a qualitative visual comparison (2x2 montage), users can effectively evaluate the performance of each method on their specific dataset. The classic method serves as a fast, accessible baseline, while StarDist and Cellpose offer more robust segmentation, particularly in handling clustered or touching cells, at the cost of requiring a configured external Python environment. This comparative approach is a best practice for validating and selecting an appropriate segmentation algorithm."""

highlights = ["A 2x2 montage is an effective visual method for comparing segmentation results against the original image.","Combining classic ImageJ macros with delegated Python tasks (StarDist, Cellpose) allows for powerful and flexible analysis pipelines.","For nuclei segmentation, classic thresholding with watershed is a viable baseline, while deep learning methods often provide more robust results for clustered objects."]

references = ["https://imagej.net/plugins/stardist","https://www.cellpose.org/","https://imagej.net/ij/docs/menus/image.html#montage"]

